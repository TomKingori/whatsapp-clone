import React from 'react';
import Logo from '../images/logo.png'

function Splash(props) {
    return (
        <div className={`splash abs abc`}>
            <img src={Logo} className={`s30 abs abc`} />
        </div>
    );
}

export default Splash;